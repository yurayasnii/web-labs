const secondsInHour = 60 * 60;
const secondsInDay = secondsInHour * 24;
const secondsInMonth = secondsInDay * 30;

alert(
  `Секунд у годині: ${secondsInHour}\n` +
  `Секунд у добі: ${secondsInDay}\n` +
  `Секунд у місяці: ${secondsInMonth}`
);
